import {StyleSheet} from 'react-native';

export const ShopCss = StyleSheet.create({
  divClicavel: {
    marginTop: 10,
    borderColor: '#fff',
    borderWidth: 2,
    width: '100%',
    height: 50,
    borderRadius: 15,
    backgroundColor: '#000',
    flexDirection: 'row',
    justifyContent: 'center',
    alignItems: 'center',
  },
  texto: {
    fontSize: 20,
    color: '#fff',
  },
});
