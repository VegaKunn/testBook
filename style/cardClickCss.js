import {StyleSheet} from 'react-native';

export const card = StyleSheet.create({
  tamanho: {
    width: '95%',
    height: 75,
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 15,
    marginTop: 15,
    borderRadius: 15,
    borderWidth: 3,
    borderColor: '#0c0',
    flexDirection: 'row',
  },
  inf: {
    backgroundColor: '#f00',
  },
});
